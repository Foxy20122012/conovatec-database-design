const getJarPath = require('./getJarPath');
const jar = getJarPath();
module.exports = function () {
    const argv = process.argv.splice(2)
    argv.unshift(jar);
    argv.unshift('-jar');
    argv.unshift('-DGRAPHVIZ_DOT=vizjs');
    return argv;
}
